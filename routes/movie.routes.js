const router = require('express').Router();

router.get('/', () => {});
router.get('/:id', () => {});
router.get('/genre/:id', () => {});
router.get('/recommend', () => {});

module.exports = router;
